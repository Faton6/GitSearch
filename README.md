# GitSearch - Advanced GitHub Leak Detection System

[![Python 3.8+](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/downloads/)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)

## 📋 Описание

GitSearch - это комплексная система автоматизированного поиска и анализа утечек конфиденциальной информации в публичных репозиториях GitHub. Система использует продвинутые техники поиска, включая AI-анализ, для обнаружения секретов, токенов, паролей и другой чувствительной информации.

### Основные возможности

- 🔍 **Многоуровневый поиск**: Поиск по репозиториям, коммитам, кодовым файлам и Gist
- 🤖 **AI-анализ**: Интеллектуальная оценка релевантности и критичности утечек
- 📊 **Детальная статистика**: Сбор метрик по репозиториям, контрибьюторам и коммитам
- 🗄️ **Интеграция с БД**: Сохранение результатов в MySQL/MariaDB
- 📈 **Генерация отчетов**: Создание детализированных отчетов в различных форматах
- ⚡ **Rate Limiting**: Интеллектуальное управление лимитами GitHub API
- 🌍 **Локализация**: Поддержка русского и английского языков
- 🔄 **Инкрементальное сканирование**: Исключение уже проверенных репозиториев

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.8 или выше
- MySQL/MariaDB (опционально, для хранения результатов)
- GitHub Personal Access Token(s)
- Docker и Docker Compose (для контейнеризации)

### Установка

1. **Клонирование репозитория**:
```bash
git clone https://github.com/yourusername/GitSearch.git
cd GitSearch
```

2. **Установка зависимостей**:
```bash
pip install -r requirements.txt
```

3. **Настройка конфигурации**:

Создайте файл `config.json`:
```json
{
    "target_list": {"Company-Name": ["search_term1", "search_term2"]},
    "leak_check_list": ["password", "token", "api_key"],
    "url_DB": "localhost",
    "token_DB": "your_db_token",
    "token_list": ["ghp_YourGitHubToken"],
    "create_report": "no",
    "start_date": "2024-01-01",
    "end_date": "2024-12-31",
    "report_type": "business"
}
```

Или создайте `.env` файл для конфиденциальных данных:
```env
GITHUB_TOKEN_1=ghp_token1
GITHUB_TOKEN_2=ghp_token2
GITHUB_CLONE_TOKEN=ghp_clone_token
URL_DB=172.32.0.97
TOKEN_DB=db_access_token
```

4. **Запуск**:
```bash
python gitsearch.py
```

### Docker запуск

```bash
docker-compose up -d
```

## 📖 Документация

### Структура проекта

```
GitSearch/
├── gitsearch.py              # Главный исполняемый файл
├── config.json               # Конфигурационный файл
├── requirements.txt          # Зависимости Python
├── Dockerfile               # Конфигурация Docker
├── docker-compose.yml       # Docker Compose конфигурация
├── src/                     # Исходный код
│   ├── constants.py         # Глобальные константы и настройки
│   ├── Connector.py         # Интерфейс работы с БД
│   ├── LeakObj.py          # Базовый класс объекта утечки
│   ├── AIObj.py            # AI-анализ утечек
│   ├── LeakAnalyzer.py     # Анализатор утечек
│   ├── api_client.py       # API клиент для БД
│   ├── api_cache.py        # Кэширование API запросов
│   ├── github_rate_limiter.py  # Управление rate limits
│   ├── report_generator.py # Генерация отчетов
│   ├── utils.py            # Утилиты
│   ├── logger.py           # Логирование
│   ├── deepscan.py         # Глубокое сканирование
│   ├── filters.py          # Фильтрация результатов
│   ├── dorks.txt           # Поисковые дорки
│   ├── exclude_list.txt    # Список исключений
│   ├── searcher/           # Модули поиска
│   │   ├── scanner.py      # Основной сканер
│   │   ├── GitStats.py     # Статистика репозиториев
│   │   └── ...
│   └── glist/              # Поиск в Gist
│       └── glist_scan.py
├── logs/                    # Логи
├── reports/                 # Сгенерированные отчеты
├── results/                 # Результаты сканирования
├── temp/                    # Временные файлы
└── test/                    # Тесты

```

### Основные модули

#### 1. **gitsearch.py**
Главный файл запуска приложения. Координирует выполнение всех этапов:
- Инициализация rate limiter
- Загрузка конфигурации и целей
- Запуск сканирования (List, Gist, GitHub)
- Глубокое сканирование (DeepScan)
- Повторное сканирование (Re-scan)
- Сохранение результатов

#### 2. **Connector.py**
Управляет взаимодействием с базой данных:
- `dump_to_DB()` - Сохранение результатов в БД
- `dump_from_DB()` - Загрузка данных из БД
- `update_existing_leak()` - Обновление существующих записей
- `safe_encode_data()` - Безопасное кодирование данных

#### 3. **LeakObj.py**
Базовый класс для всех типов утечек:
- Унифицированный интерфейс для различных типов объектов
- AI-анализ с помощью интеграции AIObj
- Расчет уровня критичности
- Генерация отчетов

#### 4. **AIObj.py**
Интеллектуальный анализ утечек:
- Оценка релевантности компании
- Определение вероятности истинного/ложного срабатывания
- Анализ профиля страны
- Оценка профитабельности утечки

#### 5. **github_rate_limiter.py**
Управление лимитами GitHub API:
- Мониторинг rate limits
- Автоматическое переключение токенов
- Умное ожидание при исчерпании лимитов
- Статистика использования API

#### 6. **report_generator.py**
Генерация различных типов отчетов:
- Business отчеты (высокоуровневые)
- Technical отчеты (детальные)
- Consolidated отчеты (сводные)

## 🔧 Конфигурация

### config.json

```json
{
    "target_list": {
        "CompanyName": ["search_term1", "search_term2", "search_term3"]
    },
    "leak_check_list": ["password", "token", "secret", "api_key"],
    "url_DB": "database_host",
    "token_DB": "database_access_token",
    "token_list": ["github_token_1", "github_token_2"],
    "create_report": "yes|no",
    "start_date": "YYYY-MM-DD",
    "end_date": "YYYY-MM-DD",
    "report_type": "business|technical|consolidated"
}
```

### Переменные окружения (.env)

```env
# GitHub токены (можно несколько)
GITHUB_TOKEN_1=ghp_xxxxxxxxxxxxx
GITHUB_TOKEN_2=ghp_xxxxxxxxxxxxx
GITHUB_CLONE_TOKEN=ghp_xxxxxxxxxxxxx

# База данных
URL_DB=172.32.0.97
TOKEN_DB=your_db_token

# AI провайдеры (опционально)
OPENAI_API_KEY=sk-xxxxxxxxxxxxx
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxx
```

### constants.py - ключевые настройки

```python
# Таймауты сканирования
MAX_TIME_TO_SCAN_BY_UTIL_DEFAULT = 100  # секунды
MAX_TIME_TO_SCAN_BY_UTIL_DEEP = 3000
MAX_TIME_TO_CLONE = 500

# GitHub API
GITHUB_REQUEST_COOLDOWN = 60.0
GITHUB_REQUEST_RATE_LIMIT = 10.0
GITHUB_REPO_COUNT_AT_REQUEST_LIMIT = 1000

# AI анализ
AI_ANALYSIS_ENABLED = True
AI_ANALYSIS_TIMEOUT = 30
AI_COMPANY_RELEVANCE_THRESHOLD = 0.5
AI_TRUE_POSITIVE_THRESHOLD = 0.6

# Размеры и лимиты
REPO_MAX_SIZE = 300000  # KB
MAX_UTIL_RES_LINES = 200
MAX_LINE_LEAK_LEN = 100
```

## 🎯 Использование

### Базовое сканирование

```bash
python gitsearch.py
```

### Создание отчета из БД

Установите в `config.json`:
```json
{
    "create_report": "yes",
    "start_date": "2024-01-01",
    "end_date": "2024-12-31",
    "report_type": "business"
}
```

Запустите:
```bash
python gitsearch.py
```

### Запуск тестов

```bash
pytest -v
```

или

```bash
python -m pytest test/
```

## 📊 Типы отчетов

### Business Report
- Краткое резюме для руководства
- Статистика по категориям утечек
- Графики и диаграммы
- Приоритезация по критичности

### Technical Report
- Детальная информация по каждой утечке
- Контекст кода
- Рекомендации по исправлению
- Технические метрики

### Consolidated Report
- Сводная информация по всем компаниям
- Тренды и динамика
- Сравнительный анализ

## 🔐 Безопасность

### Рекомендации

1. **Храните токены в безопасности**: Используйте `.env` файл и добавьте его в `.gitignore`
2. **Используйте токены с минимальными правами**: Для поиска достаточно `public_repo` scope
3. **Ротация токенов**: Регулярно обновляйте GitHub токены
4. **Шифрование БД**: Используйте SSL/TLS для подключения к БД
5. **Аудит логов**: Регулярно проверяйте логи на подозрительную активность

### Исключения

Добавьте репозитории в `exclude_list.txt` для исключения из сканирования:
```
owner/repo-name
another-owner/another-repo
```

## 🐛 Отладка

### Логи

Логи сохраняются в директории `logs/` с именами `log_YYYY_MM_DD.log`

Уровни логирования:
- `DEBUG`: Детальная информация для отладки
- `INFO`: Общая информация о работе
- `WARNING`: Предупреждения
- `ERROR`: Ошибки выполнения
- `CRITICAL`: Критические ошибки

### Распространенные проблемы

#### Rate Limit Exceeded
```
Решение: Добавьте больше GitHub токенов в конфигурацию
```

#### Connection to DB Failed
```
Решение: Проверьте настройки подключения в config.json или .env
```

#### Clone Timeout
```
Решение: Увеличьте MAX_TIME_TO_CLONE в constants.py
```

## 📝 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 👥 Авторы

- **Faton6** - *Initial work* - [GitHub](https://github.com/Faton6)

---

**Внимание**: Этот инструмент предназначен для легитимного использования в целях обеспечения безопасности. Убедитесь, что у вас есть разрешение на сканирование целевых организаций.
